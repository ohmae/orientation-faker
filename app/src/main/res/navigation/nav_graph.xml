<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/MainFragment"
    >

    <fragment
        android:id="@+id/MainFragment"
        android:name="net.mm2d.orientation.view.MainFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_main"
        >
        <action
            android:id="@+id/action_MainFragment_to_DetailedSettingsFragment"
            app:destination="@id/DetailedSettingsFragment"
            />
        <action
            android:id="@+id/action_MainFragment_to_EachAppFragment"
            app:destination="@id/EachAppFragment"
            />
    </fragment>
    <fragment
        android:id="@+id/DetailedSettingsFragment"
        android:name="net.mm2d.orientation.view.DetailedSettingsFragment"
        android:label="@string/menu_title_detailed_settings"
        tools:layout="@layout/fragment_detailed_settings"
        >

    </fragment>
    <fragment
        android:id="@+id/EachAppFragment"
        android:name="net.mm2d.orientation.view.EachAppFragment"
        android:label="@string/menu_title_setting_for_each_app"
        tools:layout="@layout/fragment_each_app"
        >

    </fragment>
</navigation>
